#include "main.h"

/**
 * main - tests functions that checkes if number is postive or negative
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	i = 0;

	postive_or_negative(i);

	return (0);
}
